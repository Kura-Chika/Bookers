<%= form_with model: @book do |f| %>
  <div>
    <%= f.label :"Title:" %>
    <%= @book.title %>
  </div>
  <div>
    <%= f.label :"Body:" %>
    <%= @book.body %>
  </div>
  <%= link_to "Edit", edit_book_path(@book.id) %>
  <%= link_to "Back", "/books" %>

<% end %>